let cheerio,axios;_47c‍.x([["getUrl",()=>getUrl]]);_47c‍.w("cheerio",[["default",["cheerio"],function(v){cheerio=v}]]);_47c‍.w("axios",[["default",["axios"],function(v){axios=v}]]);

const anime = [];
       async function getUrl() {
  _47c‍.g.console.log(anime.length);
  if (anime.length == 0) {
    const link = "https://otakudesu.tv/ongoing-anime/";
    const result = await axios.get(link);
    const $ = cheerio.load(result.data);
    anime.push({
      date: Date.now(),
      ongoing: [],
    });
    $(".detpost").each(function () {
      anime[0].ongoing.push({
        judul: $(this).find(".jdlflm").html(),
        tanggal: $(this).find(".newnime").html(),
        link: $(this).find("a").attr("href"),
      });
    });
  }
  return anime;
}
