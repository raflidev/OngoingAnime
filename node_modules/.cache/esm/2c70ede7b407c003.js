let getUrl,express,CryptoJS;_b40‍.w("./scrapping",[["getUrl",["getUrl"],function(v){getUrl=v}]]);_b40‍.w("express",[["default",["express"],function(v){express=v}]]);_b40‍.w("crypto-js",[["default",["CryptoJS"],function(v){CryptoJS=v}]]);



const app = express();
const port = 3000;
app.set("view engine", "ejs");
var hash = CryptoJS.Base64("asd");
_b40‍.g.console.log(hash.toString(CryptoJS.enc.Base64));
async function main() {
  const result = await getUrl();
  return result;
}
app.get("/", async (req, res) => {
  const tampilData = await main();
  res.json(tampilData);
});

app.get("/home", async (req, res) => {
  const tampilData = await main();
  res.render("home", {
    anime: tampilData,
  });
  // console.log(path.join(view + "home.html"));
  // const tampilData = await main();
  // res.sendFile(path.join(view + "home.html"));
  // res.send({ user: "tobi" });
});

app.listen(port, () => {
  console.log(`Aplikasi jalan di port http://localhost:${port}`);
});
